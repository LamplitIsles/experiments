import { afterEach, describe, expect, test } from "bun:test";
import { mkdir, mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { configText, extractContext, loadState, plistText, prepareSetup, scan, searchFilters, setupPaths } from "./core";

const roots:string[]=[]; afterEach(async()=>{await Promise.all(roots.splice(0).map(root=>rm(root,{recursive:true,force:true})));});
async function fixture(){const root=await mkdtemp(join(tmpdir(),"flicklog-test-"));roots.push(root);const home=join(root,"codex"), sessions=join(home,"sessions","2026");await mkdir(sessions,{recursive:true});await writeFile(join(home,"session_index.jsonl"),JSON.stringify({id:"s1",thread_name:"named"})+"\n");const path=join(sessions,"s1.jsonl");const record=(x:unknown)=>JSON.stringify(x)+"\n";const meta={type:"session_meta",payload:{id:"s1",session_id:"s1",cwd:"/project",source:"cli",thread_source:"user"}};const user={type:"response_item",timestamp:"2026-01-01T00:00:00Z",payload:{type:"message",role:"user",content:[{type:"input_text",text:"你好 mixed code"}],internal_chat_message_metadata_passthrough:{content_item_kinds:["user.text"]}}};const commentary={type:"response_item",payload:{type:"message",role:"assistant",phase:"commentary",content:[{type:"output_text",text:"working"}],internal_chat_message_metadata_passthrough:{content_item_kinds:["unknown"]}}};const reasoning={type:"response_item",payload:{type:"message",role:"assistant",phase:"analysis",content:[{type:"output_text",text:"secret"}],internal_chat_message_metadata_passthrough:{content_item_kinds:["unknown"]}}};const final={type:"response_item",payload:{type:"message",role:"assistant",phase:"final_answer",content:[{type:"output_text",text:"done"}],internal_chat_message_metadata_passthrough:{content_item_kinds:["unknown"]}}};await writeFile(path,[meta,user,commentary,reasoning,final].map(record).join(""));return {root,home,path,final,record};}
describe("incremental Codex projection",()=>{test("indexes only eligible records, preserves source ordinals, and appends",async()=>{const f=await fixture(), batches:any[]=[];const env={CODEX_HOME:f.home,FLICKLOG_STATE_DIR:join(f.root,"state")};expect((await scan(env,async x=>{batches.push(x);})).indexed).toBe(3);expect(batches[0].map((x:any)=>[x.role,x.phase,x.sourceRecordIndex])).toEqual([["user",undefined,1],["assistant","commentary",2],["assistant","final_answer",4]]);expect((await scan(env,async x=>{batches.push(x);})).indexed).toBe(0);await writeFile(f.path,f.record(f.final),{flag:"a"});expect((await scan(env,async x=>{batches.push(x);})).indexed).toBe(1);});
test("does not commit an incomplete trailing record",async()=>{const f=await fixture(), env={CODEX_HOME:f.home,FLICKLOG_STATE_DIR:join(f.root,"state")};await writeFile(f.path,"{\"type\":",{flag:"a"});await scan(env,async()=>{});const state=await loadState(join(env.FLICKLOG_STATE_DIR,"scan-state.json"));const committed=state.sources[f.path].offset;expect(committed).toBeLessThan((await (await import("node:fs/promises")).stat(f.path)).size);});});
describe("source context and setup contract",()=>{test("excludes reasoning, controls tools, and marks truncation",()=>{const text=[JSON.stringify({type:"response_item",payload:{type:"message",role:"user",content:[{type:"input_text",text:"question"}],internal_chat_message_metadata_passthrough:{content_item_kinds:["user.text"]}}}),JSON.stringify({type:"response_item",payload:{type:"message",role:"assistant",phase:"analysis",content:[{type:"output_text",text:"secret"}],internal_chat_message_metadata_passthrough:{content_item_kinds:["unknown"]}}}),JSON.stringify({type:"response_item",payload:{type:"function_call_output",output:"x".repeat(40)}})].join("\n")+"\n";expect(extractContext(text,0,false).map(x=>x.kind)).toEqual(["message"]);const items=extractContext(text,0,true,8,10);expect(items.some(x=>x.kind==="tool"&&x.truncated)).toBe(true);expect(items.some(x=>x.content.includes("secret"))).toBe(false);});
test("keeps device filtering when all projects is selected",()=>{expect(searchFilters("/project",false)).toHaveLength(2);expect(searchFilters("/project",true)).toHaveLength(1);expect(searchFilters("/project",false)[1]).toContain("/project");});
test("renders a dedicated loopback LaunchAgent",async()=>{const root=await mkdtemp(join(tmpdir(),"flicklog-setup-"));roots.push(root);const env={FLICKLOG_STATE_DIR:root,FLICKLOG_LAUNCH_AGENTS_DIR:join(root,"agents")};const p=await prepareSetup(env,"/opt/homebrew/bin/meilisearch");expect(configText(p,await readFile(p.key,"utf8"))).toContain("127.0.0.1:7701");expect(plistText(p,"/opt/homebrew/bin/meilisearch")).toContain("dev.flicklog.meilisearch");expect(setupPaths(env).database).toContain("meilisearch-data");});});
